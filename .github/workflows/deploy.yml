name: Deploy

# Manual-only: this repo is public, so production CD is NOT coupled to pushes.
# A merge to main must never touch production on its own — deploy is triggered
# deliberately via the GitHub "Run workflow" button (or `gh workflow run`).
on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Authenticate to GCP
        uses: google-github-actions/auth@v2
        with:
          credentials_json: '${{ secrets.GCP_SA_KEY }}'

      - name: Setup gcloud
        uses: google-github-actions/setup-gcloud@v2

      - name: Deploy to VM
        run: |
          gcloud compute ssh ${{ secrets.VM_SSH_USER }}@${{ secrets.VM_NAME }} \
            --zone=${{ secrets.GCP_ZONE }} \
            --project=${{ secrets.GCP_PROJECT_ID }} \
            --tunnel-through-iap \
            --command="bash ~/lox-brain/infra/deploy.sh 2>&1 | tee /tmp/deploy.log"

      - name: Health check
        run: |
          gcloud compute ssh ${{ secrets.VM_SSH_USER }}@${{ secrets.VM_NAME }} \
            --zone=${{ secrets.GCP_ZONE }} \
            --project=${{ secrets.GCP_PROJECT_ID }} \
            --tunnel-through-iap \
            --command="systemctl is-active lox-watcher && tail -1 /tmp/deploy.log | grep -q DEPLOY_SUCCESS"
